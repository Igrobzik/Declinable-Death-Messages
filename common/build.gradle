plugins {
    id 'java-library'
}

base {
    archivesName = "declinabledeathmessages-common"
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

tasks.withType(JavaCompile).configureEach {
    options.release = 21
}